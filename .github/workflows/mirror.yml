name: Mirror Distributions
on:
  workflow_dispatch:
  schedule:
    - cron: '00 1 * * 1-5'

jobs:
  mirror-node:
    uses: ./_mirror-distribution.yml
    with:
      name: Node.js
      distribution: node
    secrets: inherit

  mirror-yarn:
    uses: ./_mirror-distribution.yml
    with:
      name: Yarn
      distribution: yarn
    secrets: inherit

  mirror-npm:
    uses: ./_mirror-distribution.yml
    with:
      name: NPM
      distribution: npm
    secrets: inherit
